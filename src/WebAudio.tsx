const WebAudio = () => {
  // WOscillator on WebAudio  "this will go brrr in sine"
  const oscillator = new AudioContext();

  /* do you really neeed it? no  that's why i'm gonna complete it when i feel the need for it */
};

export default WebAudio;
